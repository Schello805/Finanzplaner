"use client";
import Image from "next/image";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { LockKeyhole } from "lucide-react";

export default function LoginPage(){const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:form.get("username"),password:form.get("password")})});const body=await response.json();setBusy(false);if(!response.ok){setError(body.error);return}router.push(body.mustChangePassword?"/passwort-aendern":"/");router.refresh()}
return <div className="mx-auto flex min-h-[80vh] max-w-md items-center"><section className="card w-full p-7"><div className="text-center"><Image src="/logo-finanzplaner.jpeg" alt="" width={72} height={72} className="mx-auto rounded-2xl"/><h1 className="mt-4 text-2xl font-bold">Willkommen beim Finanzplaner</h1><p className="mt-2 text-sm muted">Melde dich in deinem Familienhaushalt an.</p></div><form onSubmit={submit} className="mt-7 space-y-4"><label className="block"><span className="text-sm font-semibold">Benutzername</span><input name="username" autoComplete="username" required className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4"/></label><label className="block"><span className="text-sm font-semibold">Passwort</span><input name="password" type="password" autoComplete="current-password" required className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4"/></label>{error&&<div role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</div>}<button disabled={busy} className="btn-primary w-full"><LockKeyhole size={18}/>{busy?"Anmeldung läuft …":"Anmelden"}</button></form><p className="mt-5 text-center text-xs muted">Nur im vertrauenswürdigen lokalen Netzwerk verwenden.</p></section></div>}
