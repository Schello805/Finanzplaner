"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {KeyRound} from "lucide-react";
export default function ForgotPasswordPage(){const[message,setMessage]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/auth/request-password-reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({login:form.get("login")})});const body=await response.json();setBusy(false);if(response.ok)setMessage(body.message);else setError(body.error)}return <div className="mx-auto flex min-h-[80vh] max-w-md items-center"><section className="card w-full p-7"><KeyRound className="text-[var(--primary)]"/><h1 className="mt-4 text-2xl font-bold">Passwort zurücksetzen</h1><p className="mt-2 text-sm leading-6 muted">Gib deinen Benutzernamen oder deine hinterlegte E-Mail-Adresse ein.</p>{message?<div role="status" className="mt-6 rounded-xl bg-[var(--surface-soft)] p-4 text-sm leading-6">{message}</div>:<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Benutzername oder E-Mail-Adresse<input name="login" required autoComplete="username" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4"/></label>{error&&<div role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</div>}<button disabled={busy} className="btn-primary w-full">{busy?"E-Mail wird vorbereitet …":"Reset-Link anfordern"}</button></form>}<Link href="/anmelden" className="btn-secondary mt-5 w-full">Zurück zur Anmeldung</Link></section></div>}
