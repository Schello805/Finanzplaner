import {NextResponse} from "next/server";import {eq} from "drizzle-orm";import {z} from "zod";import {db} from "@/db";import {userPreferences} from "@/db/schema";import {requireUser} from "@/lib/current-user";import {writeAudit} from "@/lib/audit";
const schema=z.object({automaticCategorization:z.boolean(),automaticInsights:z.boolean(),aiPrivacyMode:z.enum(["minimal","full_text"])});
const defaults={automaticCategorization:false,automaticInsights:false,aiPrivacyMode:"minimal" as const};
export async function GET(){try{const user=await requireUser();const[row]=await db.select().from(userPreferences).where(eq(userPreferences.userId,user.userId)).limit(1);return NextResponse.json(row??defaults)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Einstellungen konnten nicht geladen werden."},{status:401})}}
export async function PUT(request:Request){try{const user=await requireUser();const body=schema.parse(await request.json());await db.insert(userPreferences).values({userId:user.userId,...body}).onConflictDoUpdate({target:userPreferences.userId,set:{...body,updatedAt:new Date()}});await writeAudit("ai-consent","Persönliche KI-Einwilligungen wurden geändert.",{userId:user.userId,metadata:{automaticCategorization:body.automaticCategorization,automaticInsights:body.automaticInsights,aiPrivacyMode:body.aiPrivacyMode}});return NextResponse.json({ok:true,...body})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Einstellungen konnten nicht gespeichert werden."},{status:400})}}
