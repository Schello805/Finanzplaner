import "server-only";import {and,eq,or} from "drizzle-orm";import {db} from "@/db";import {accounts,householdMembers} from "@/db/schema";
export async function memberAndVisibleAccountIds(userId:string){const[member]=await db.select().from(householdMembers).where(eq(householdMembers.userId,userId)).limit(1);if(!member)throw new Error("Kein Haushalt eingerichtet.");const rows=await db.select({id:accounts.id}).from(accounts).where(and(eq(accounts.householdId,member.householdId),or(eq(accounts.kind,"joint"),eq(accounts.ownerMemberId,member.id))));return{member,accountIds:rows.map(r=>r.id)}}
