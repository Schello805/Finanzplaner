"use client";
import {useEffect,useState} from "react";import {Download,Filter,Search} from "lucide-react";import {PageHeader} from "@/components/page-header";
type Log={id:string;createdAt:string;level:string;eventType:string;message:string;username:string|null};
export default function LogPage(){const[logs,setLogs]=useState<Log[]>([]);const[q,setQ]=useState("");const[level,setLevel]=useState("");const[sort,setSort]=useState("desc");const[error,setError]=useState("");useEffect(()=>{const timer=setTimeout(()=>{const p=new URLSearchParams({q,level,sort});fetch(`/api/admin/logs?${p}`).then(r=>r.json()).then(b=>Array.isArray(b)?setLogs(b):setError(b.error))},200);return()=>clearTimeout(timer)},[q,level,sort]);return <div className="space-y-7"><PageHeader eyebrow="Admin · Betrieb" title="Systemprotokoll" description="Ein bereinigtes Protokoll mit 90 Tagen Aufbewahrung." action={<button className="btn-secondary" onClick={()=>window.print()}><Download size={17}/> Drucken</button>}/>{error&&<div role="alert" className="rounded-xl bg-red-50 p-4 text-sm text-red-800">{error}</div>}<section className="card overflow-hidden"><div className="flex flex-wrap gap-2 border-b border-[var(--border)] p-4"><label className="flex min-h-11 min-w-64 flex-1 items-center gap-2 rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Protokoll durchsuchen" className="w-full border-0 bg-transparent outline-none"/></label><label className="flex items-center gap-2"><Filter size={17}/><select value={level} onChange={e=>setLevel(e.target.value)} className="min-h-11 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3"><option value="">Alle Stufen</option><option value="info">Info</option><option value="warning">Hinweis</option><option value="error">Fehler</option></select></label><select aria-label="Sortierung" value={sort} onChange={e=>setSort(e.target.value)} className="min-h-11 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3"><option value="desc">Neueste zuerst</option><option value="asc">Älteste zuerst</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead className="bg-[var(--surface-soft)] text-left"><tr>{["Zeitpunkt","Stufe","Ereignis","Benutzer","Meldung"].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody>{logs.map(row=><tr key={row.id} className="border-t border-[var(--border)]"><td className="px-5 py-4">{new Intl.DateTimeFormat("de-DE",{dateStyle:"short",timeStyle:"medium"}).format(new Date(row.createdAt))}</td><td className="px-5 py-4">{row.level}</td><td className="px-5 py-4">{row.eventType}</td><td className="px-5 py-4">{row.username??"System"}</td><td className="px-5 py-4">{row.message}</td></tr>)}{!logs.length&&<tr><td colSpan={5} className="px-5 py-10 text-center muted">Keine Protokolleinträge gefunden.</td></tr>}</tbody></table></div></section></div>}
