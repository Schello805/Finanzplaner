"use client";
import{useEffect,useState}from"react";
import{Download,Filter,Search}from"lucide-react";
import{PageHeader}from"@/components/page-header";
type Log={id:string;createdAt:string;level:string;eventType:string;username:string|null;message:string};
export default function LogPage(){const[logs,setLogs]=useState<Log[]>([]),[q,setQ]=useState(""),[level,setLevel]=useState(""),[sort,setSort]=useState("desc"),[error,setError]=useState("");useEffect(()=>{const timer=setTimeout(()=>{const p=new URLSearchParams({q,level,sort});fetch(`/api/admin/logs?${p}`).then(r=>r.json()).then(b=>Array.isArray(b)?setLogs(b):setError(b.error))},200);return()=>clearTimeout(timer)},[q,level,sort]);return <div className="space-y-7"><PageHeader eyebrow="Admin · Betrieb" title="Systemprotokoll" description="Ein bereinigtes Protokoll mit 90 Tagen Aufbewahrung." action={<button className="btn-secondary" onClick={()=>window.print()}><Download size={17}/>Drucken</button>}/>{error&&<div role="alert" className="rounded-xl bg-red-50 p-4 text-sm text-red-800">{error}</div>}<section className="card overflow-hidden"><div className="grid gap-2 border-b border-[var(--border)] p-4 sm:grid-cols-2 lg:grid-cols-[1fr_auto_auto]"><label className="flex min-h-11 min-w-0 items-center gap-2 rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Protokoll durchsuchen" className="min-w-0 w-full border-0 bg-transparent outline-none"/></label><label className="flex min-h-11 items-center gap-2 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3"><Filter size={17}/><select value={level} onChange={e=>setLevel(e.target.value)} className="min-w-0 flex-1 bg-transparent"><option value="">Alle Stufen</option><option value="info">Info</option><option value="warning">Hinweis</option><option value="error">Fehler</option></select></label><select aria-label="Sortierung" value={sort} onChange={e=>setSort(e.target.value)} className="min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 sm:col-span-2 lg:col-span-1 lg:w-auto"><option value="desc">Neueste zuerst</option><option value="asc">Älteste zuerst</option></select></div><div className="responsive-table-wrap overflow-x-auto"><table className="transaction-table w-full min-w-[760px] text-sm"><thead className="bg-[var(--surface-soft)] text-left"><tr>{["Zeitpunkt","Stufe","Ereignis","Benutzer","Meldung"].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody>{logs.map(row=><tr key={row.id} className="border-t border-[var(--border)]"><td data-label="Zeitpunkt" className="px-5 py-4">{new Intl.DateTimeFormat("de-DE",{dateStyle:"short",timeStyle:"medium"}).format(new Date(row.createdAt))}</td><td data-label="Stufe" className="px-5 py-4">{row.level}</td><td data-label="Ereignis" className="px-5 py-4 break-words">{row.eventType}</td><td data-label="Benutzer" className="px-5 py-4 break-words">{row.username??"System"}</td><td data-label="Meldung" className="px-5 py-4 break-words">{row.message}</td></tr>)}{!logs.length&&<tr><td colSpan={5} className="px-5 py-10 text-center muted">Keine Protokolleinträge gefunden.</td></tr>}</tbody></table></div></section></div>}
