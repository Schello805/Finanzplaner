"use client";import {FormEvent,useEffect,useState} from "react";import {Mail,UserRound} from "lucide-react";import {PageHeader} from "@/components/page-header";
export default function ProfilePage(){const[email,setEmail]=useState("");const[displayName,setDisplayName]=useState("");const[username,setUsername]=useState("");const[message,setMessage]=useState("");const[busy,setBusy]=useState(true);useEffect(()=>{fetch("/api/user/profile").then(r=>r.json()).then(body=>{setEmail(body.email??"");setDisplayName(body.displayName??"");setUsername(body.username??"")}).finally(()=>setBusy(false))},[]);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const response=await fetch("/api/user/profile",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const body=await response.json();setMessage(response.ok?"E-Mail-Adresse gespeichert. Der Passwort-Reset ist jetzt einsatzbereit.":body.error);setBusy(false)}return <div className="space-y-7"><PageHeader eyebrow="Einstellungen · Persönlich" title="Profil" description="Verwalte die Kontaktadresse für sicherheitsrelevante Nachrichten."/><section className="card p-6"><div className="flex items-center gap-3"><UserRound className="text-[var(--primary)]"/><div><h2 className="font-bold">{displayName||"Benutzerprofil"}</h2><p className="text-sm muted">Benutzername: {username||"–"}</p></div></div><form onSubmit={submit} className="mt-6"><label className="block text-sm font-semibold">E-Mail-Adresse für Passwort-Reset<div className="relative mt-2"><Mail size={18} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 muted"/><input type="email" required value={email} onChange={event=>setEmail(event.target.value)} autoComplete="email" className="min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] pl-10 pr-4" placeholder="name@beispiel.de"/></div></label><p className="mt-2 text-xs leading-5 muted">An diese Adresse werden ausschließlich sicherheitsrelevante Links ohne Finanzdaten gesendet.</p>{message&&<div role="status" className="mt-4 rounded-xl bg-[var(--surface-soft)] p-4 text-sm">{message}</div>}<button disabled={busy} className="btn-primary mt-5">E-Mail-Adresse speichern</button></form></section></div>}
