"use client";
import {useEffect,useState} from "react";import {ChevronDown,ShieldCheck,Sparkles} from "lucide-react";
type Preview={count:number;provider:"openai"|"gemini";model:string;privacyMode:"minimal"|"full_text";transactions:Array<{id:string;date:string;amount:number;currency:string;merchant?:string;purpose?:string}>;cost:{approximateInputTokens:number;lowEur:number;highEur:number}};
export function AiCategorizationPanel({onApplied}:{onApplied:()=>void}){const[preview,setPreview]=useState<Preview|null>(null);const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[mode,setMode]=useState<"minimal"|"full_text">("minimal");async function load(nextMode=mode){const r=await fetch(`/api/ai/categorize?privacyMode=${nextMode}`);const b=await r.json();if(r.ok)setPreview(b)}useEffect(()=>{fetch("/api/ai/categorize?privacyMode=minimal").then(r=>r.json()).then(b=>{if(b.count)setPreview(b)})},[]);async function changeMode(next:"minimal"|"full_text"){setMode(next);await load(next)}async function analyze(){if(!preview)return;setBusy(true);const r=await fetch("/api/ai/categorize",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({ids:preview.transactions.map(t=>t.id),privacyMode:mode})});const b=await r.json();setBusy(false);if(!r.ok){setMessage(b.error);return}setMessage(`${b.applied} sichere Zuordnungen übernommen. ${b.suggestions.length} Vorschläge müssen manuell geprüft werden. Geschätzte Kosten: ${Number(b.estimatedCostEur).toLocaleString("de-DE",{style:"currency",currency:"EUR",minimumFractionDigits:4})}.`);await load();onApplied()}
if(!preview?.count)return null;return <section className="card border-[color-mix(in_srgb,var(--primary)_35%,var(--border))] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[var(--surface-soft)] text-[var(--primary)]"><Sparkles size={20}/></div><div><h2 className="font-bold">{preview.count} nicht zugeordnete Umsätze</h2><p className="mt-1 text-sm muted">Mit {preview.provider==="openai"?"OpenAI":"Gemini"} · {preview.model} analysieren</p></div></div><button onClick={()=>setOpen(v=>!v)} className="btn-secondary">Details & Kosten <ChevronDown size={16}/></button></div>{open&&<div className="mt-5 border-t border-[var(--border)] pt-5"><div className="grid gap-4 sm:grid-cols-2"><label className="rounded-xl bg-[var(--surface-soft)] p-4 text-sm"><input type="radio" checked={mode==="minimal"} onChange={()=>changeMode("minimal")} className="mr-2 accent-[var(--primary)]"/><strong>Datensparsam</strong><span className="mt-1 block muted">Namen und Identifikatoren werden bereinigt.</span></label><label className="rounded-xl bg-[var(--surface-soft)] p-4 text-sm"><input type="radio" checked={mode==="full_text"} onChange={()=>changeMode("full_text")} className="mr-2 accent-[var(--primary)]"/><strong>Beste Erkennung</strong><span className="mt-1 block muted">Vollständiger Buchungstext, weiterhin ohne IBAN.</span></label></div><div className="mt-4 rounded-xl border border-[var(--border)] p-4"><div className="flex items-center gap-2 text-sm font-bold"><ShieldCheck size={17}/> Diese Daten werden übertragen</div><div className="mt-3 max-h-52 space-y-2 overflow-auto text-xs">{preview.transactions.map(t=><div key={t.id} className="rounded-lg bg-[var(--surface-soft)] p-3">{t.date} · {t.amount.toLocaleString("de-DE",{style:"currency",currency:t.currency})} · {t.merchant??"Unbekannt"}<div className="mt-1 muted">{t.purpose}</div></div>)}</div></div><p className="mt-4 text-sm"><strong>Kostenschätzung:</strong> {preview.cost.lowEur.toLocaleString("de-DE",{style:"currency",currency:"EUR",minimumFractionDigits:4})}–{preview.cost.highEur.toLocaleString("de-DE",{style:"currency",currency:"EUR",minimumFractionDigits:4})} · ca. {preview.cost.approximateInputTokens.toLocaleString("de-DE")} Eingabe-Tokens</p><button disabled={busy} onClick={analyze} className="btn-primary mt-4">{busy?"KI analysiert …":"Nicht zugeordnete Umsätze mit KI analysieren"}</button></div>}{message&&<div role="status" className="mt-4 rounded-xl bg-[var(--surface-soft)] p-4 text-sm">{message}</div>}</section>}
