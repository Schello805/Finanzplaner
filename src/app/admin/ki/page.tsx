"use client";
import {FormEvent,useEffect,useState} from "react";import {Bot,CheckCircle2} from "lucide-react";import {PageHeader} from "@/components/page-header";
type Config={defaultProvider:"openai"|"gemini";openai:{model?:string;configured?:boolean};gemini:{model?:string;configured?:boolean}};
export default function AdminAi(){const[config,setConfig]=useState<Config>({defaultProvider:"openai",openai:{model:"gpt-5-mini"},gemini:{model:"gemini-3.7-flash"}});const[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/settings").then(r=>r.json()).then(b=>{if(b.ai)setConfig(b.ai)})},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={section:"ai",defaultProvider:f.get("defaultProvider"),openai:{model:f.get("openaiModel"),apiKey:f.get("openaiKey")||undefined},gemini:{model:f.get("geminiModel"),apiKey:f.get("geminiKey")||undefined}};const r=await fetch("/api/admin/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const b=await r.json();setMessage(r.ok?"Einstellungen gespeichert.":b.error)}return <form onSubmit={submit} className="space-y-7"><PageHeader eyebrow="Admin · KI" title="KI-Anbieter" description="Schlüssel werden verschlüsselt gespeichert und nie wieder vollständig angezeigt."/><section className="grid gap-4 lg:grid-cols-2">{([['openai','OpenAI',config.openai],['gemini','Google Gemini',config.gemini]] as const).map(([id,name,item])=><article className="card p-5" key={id}><div className="flex justify-between"><Bot className="text-[var(--primary)]"/><label className="text-sm font-semibold"><input type="radio" name="defaultProvider" value={id} defaultChecked={config.defaultProvider===id} className="mr-2 accent-[var(--primary)]"/>Standard</label></div><h2 className="mt-4 font-bold">{name}</h2><label className="mt-4 block text-sm font-semibold">API-Schlüssel<input name={`${id}Key`} type="password" placeholder={item.configured?"Gespeichert – leer lassen zum Beibehalten":"API-Schlüssel eingeben"} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label><label className="mt-4 block text-sm font-semibold">Modell<input name={`${id}Model`} defaultValue={item.model} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label><div className="mt-4 flex items-center gap-2 text-sm muted"><CheckCircle2 size={16}/>{item.configured?"Schlüssel eingerichtet":"Noch nicht eingerichtet"}</div></article>)}</section>{message&&<div role="status" className="rounded-xl bg-[var(--surface-soft)] p-4 text-sm">{message}</div>}<button className="btn-primary">KI-Einstellungen speichern</button><article className="card p-5"><h2 className="font-bold">Geschätzte Nutzung im September</h2><p className="mt-2 text-2xl font-bold">0,00 €</p><p className="mt-1 text-sm muted">Verbindliche Limits verwaltest du beim jeweiligen Anbieter.</p></article></form>}
