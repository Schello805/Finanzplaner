"use client";
import {FormEvent,useEffect,useState} from "react";import {Bot,CheckCircle2} from "lucide-react";import {PageHeader} from "@/components/page-header";import {PasswordInput} from "@/components/password-input";
type Provider={model?:string;configured?:boolean;inputPricePerMillion?:number;outputPricePerMillion?:number};type Config={defaultProvider:"openai"|"gemini";openai:Provider;gemini:Provider};
export default function AdminAi(){const[config,setConfig]=useState<Config>({defaultProvider:"openai",openai:{model:"gpt-5-mini",inputPricePerMillion:0.25,outputPricePerMillion:2},gemini:{model:"gemini-3.7-flash"}});const[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/settings").then(r=>r.json()).then(b=>{if(b.ai)setConfig(b.ai)})},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const provider=(id:"openai"|"gemini")=>({model:f.get(`${id}Model`),apiKey:f.get(`${id}Key`)||undefined,inputPricePerMillion:Number(f.get(`${id}InputPrice`)||0),outputPricePerMillion:Number(f.get(`${id}OutputPrice`)||0)});const body={section:"ai",defaultProvider:f.get("defaultProvider"),openai:provider("openai"),gemini:provider("gemini")};const r=await fetch("/api/admin/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const b=await r.json();setMessage(r.ok?"Einstellungen gespeichert.":b.error)}return <form key={JSON.stringify(config)} onSubmit={submit} className="space-y-7"><PageHeader eyebrow="Admin · KI" title="KI-Anbieter" description="Schlüssel werden verschlüsselt gespeichert und nie wieder vollständig angezeigt."/><section className="grid gap-4 lg:grid-cols-2">{([['openai','OpenAI',config.openai],['gemini','Google Gemini',config.gemini]] as const).map(([id,name,item])=><article className="card p-5" key={id}><div className="flex justify-between"><Bot className="text-[var(--primary)]"/><label className="text-sm font-semibold"><input type="radio" name="defaultProvider" value={id} defaultChecked={config.defaultProvider===id} className="mr-2 accent-[var(--primary)]"/>Standard</label></div><h2 className="mt-4 font-bold">{name}</h2><label className="mt-4 block text-sm font-semibold">API-Schlüssel<PasswordInput name={`${id}Key`} autoComplete="off" placeholder={item.configured?"Gespeichert – leer lassen zum Beibehalten":"API-Schlüssel eingeben"} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label><label className="mt-4 block text-sm font-semibold">Modell<input name={`${id}Model`} defaultValue={item.model} className="mt-2 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label><div className="mt-4 grid grid-cols-2 gap-3"><label className="text-xs font-semibold">Eingabe €/1 Mio. Tokens<input name={`${id}InputPrice`} type="number" min="0" step="0.0001" defaultValue={item.inputPricePerMillion??0} className="mt-2 min-h-10 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label><label className="text-xs font-semibold">Ausgabe €/1 Mio. Tokens<input name={`${id}OutputPrice`} type="number" min="0" step="0.0001" defaultValue={item.outputPricePerMillion??0} className="mt-2 min-h-10 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"/></label></div><div className="mt-4 flex items-center gap-2 text-sm muted"><CheckCircle2 size={16}/>{item.configured?"Schlüssel eingerichtet":"Noch nicht eingerichtet"}</div></article>)}</section>{message&&<div role="status" className="rounded-xl bg-[var(--surface-soft)] p-4 text-sm">{message}</div>}<button className="btn-primary">KI-Einstellungen speichern</button><article className="card p-5"><h2 className="font-bold">Kosteninformationen</h2><p className="mt-2 text-sm muted">Die App berechnet Schätzungen aus den hier hinterlegten Preisen und den vom Anbieter gemeldeten Tokens. Für gpt-5-mini ergänzt die App fehlende Preise automatisch anhand der offiziellen Modellangaben. Andere Preise bitte aus der aktuellen Anbieterabrechnung übernehmen.</p></article></form>}
