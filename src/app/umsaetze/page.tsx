"use client";
import {useEffect,useMemo,useState} from "react";import {Filter,Search,SlidersHorizontal} from "lucide-react";import {PageHeader} from "@/components/page-header";
type Row={id:string;bookedOn:string;amount:string;currency:string;counterparty:string|null;purpose:string|null;categoryId:string|null;categoryName:string|null;accountName:string;excluded:boolean};type Category={id:string;name:string;parentId:string|null};
export default function TransactionsPage(){const[rows,setRows]=useState<Row[]>([]);const[categories,setCategories]=useState<Category[]>([]);const[query,setQuery]=useState("");const[error,setError]=useState("");useEffect(()=>{Promise.all([fetch("/api/transactions").then(r=>r.json()),fetch("/api/categories").then(r=>r.json())]).then(([tx,cats])=>{if(Array.isArray(tx))setRows(tx);else setError(tx.error);if(Array.isArray(cats))setCategories(cats)})},[]);const visible=useMemo(()=>{const q=query.toLocaleLowerCase("de-DE");return rows.filter(r=>!q||`${r.counterparty} ${r.purpose} ${r.categoryName}`.toLocaleLowerCase("de-DE").includes(q))},[rows,query]);async function setCategory(row:Row,categoryId:string){const response=await fetch("/api/transactions",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:row.id,categoryId:categoryId||null})});const body=await response.json();if(!response.ok){setError(body.error);return}const category=categories.find(c=>c.id===categoryId);setRows(items=>items.map(item=>item.id===row.id?{...item,categoryId:categoryId||null,categoryName:category?.name??null}:item))}
return <div className="space-y-7"><PageHeader eyebrow="Buchungen" title="Umsätze" description="Durchsuche, prüfe und kategorisiere deine Buchungen." action={<button className="btn-secondary"><SlidersHorizontal size={18}/> Kategorien verwalten</button>}/>{error&&<div role="alert" className="rounded-xl bg-red-50 p-4 text-sm text-red-800">{error}</div>}<section className="card overflow-hidden"><div className="flex flex-col gap-3 border-b border-[var(--border)] p-4 sm:flex-row"><label className="flex min-h-11 flex-1 items-center gap-2 rounded-xl border border-[var(--border)] bg-[var(--background)] px-3"><Search size={18} className="muted"/><span className="sr-only">Umsätze suchen</span><input value={query} onChange={e=>setQuery(e.target.value)} className="w-full border-0 bg-transparent outline-none" placeholder="Empfänger, Verwendungszweck oder Kategorie"/></label><button className="btn-secondary"><Filter size={17}/> Filter</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] border-collapse text-sm"><thead className="bg-[var(--surface-soft)] text-left muted"><tr>{["Datum","Empfänger","Konto","Kategorie","Betrag"].map(h=><th key={h} className="px-5 py-3 font-semibold">{h}</th>)}</tr></thead><tbody>{visible.map(row=><tr key={row.id} className="border-t border-[var(--border)] hover:bg-[var(--surface-soft)]"><td className="px-5 py-4">{new Intl.DateTimeFormat("de-DE").format(new Date(`${row.bookedOn}T00:00:00`))}</td><td className="max-w-[260px] px-5 py-4"><div className="truncate font-semibold">{row.counterparty??"Unbekannt"}</div><div className="truncate text-xs muted">{row.purpose}</div></td><td className="px-5 py-4 muted">{row.accountName}</td><td className="px-5 py-4"><select value={row.categoryId??""} onChange={e=>setCategory(row,e.target.value)} className="min-h-9 rounded-lg border border-[var(--border)] bg-[var(--surface)] px-2"><option value="">Nicht zugeordnet</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></td><td className={`px-5 py-4 text-right font-bold ${Number(row.amount)>0?"text-[var(--primary)]":""}`}>{Number(row.amount).toLocaleString("de-DE",{style:"currency",currency:row.currency})}</td></tr>)}{!visible.length&&<tr><td colSpan={5} className="px-5 py-10 text-center muted">Keine passenden Umsätze vorhanden.</td></tr>}</tbody></table></div></section></div>}
