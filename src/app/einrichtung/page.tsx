"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {Home,Users} from "lucide-react";
export default function Setup(){const router=useRouter();const[error,setError]=useState("");useEffect(()=>{fetch("/api/setup/status").then(r=>r.json()).then(x=>{if(x.configured)router.replace("/")})},[router]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({householdName:f.get("householdName"),displayName:f.get("displayName")})});const b=await r.json();if(!r.ok){setError(b.error);return}router.push("/");router.refresh()}return <div className="mx-auto max-w-xl"><section className="card p-7"><Home className="text-[var(--primary)]"/><h1 className="mt-4 text-2xl font-bold">Familienhaushalt einrichten</h1><p className="mt-2 muted">Lege den ersten Haushalt an. Weitere Erwachsene und Kinder fügst du anschließend hinzu.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Name des Haushalts<input name="householdName" required defaultValue="Familie" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4"/></label><label className="block text-sm font-semibold">Dein Anzeigename<input name="displayName" required className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4"/></label>{error&&<div role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</div>}<button className="btn-primary w-full"><Users size={18}/> Haushalt anlegen</button></form></section></div>}
